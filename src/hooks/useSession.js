/**
 * useSession — sequenza allenamento + timer + AI gating
 * TODO: estrarre da App.jsx 714-740 (startSession, advancePhase, finishSession) + 668-683 timer
 */
export function useSession() {
  return null;
}
