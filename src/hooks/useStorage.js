/**
 * useStorage — wrapper per window.storage (idb / Capacitor Preferences)
 * TODO: estrarre da App.jsx useEffect hydratation (441-486)
 * Oggi scaffold vuoto, domani migra:
 * - load o40_profile/sessions/waist/weight/custom_programs
 * - save helpers
 */
export function useStorage() {
  // placeholder — App.jsx usa direttamente window.storage
  return null;
}
